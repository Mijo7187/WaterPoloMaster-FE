/** Format dates are sent to the API in (filters store values) */
export const FILTER_DATE_FORMAT = "YYYY-MM-DD";

/** Format dates are displayed in inside filter pickers */
export const FILTER_DISPLAY_DATE_FORMAT = "DD-MM-YYYY";

/** Debounce delay (ms) before a filter change triggers a refetch */
export const FILTER_DEBOUNCE_MS = 300;

/** Default antd Col span of a filter field */
export const FILTER_DEFAULT_COL_SPAN = 4;
